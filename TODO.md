# TODO

- [ ] Сделать безопасный разбор callback-данных: отказаться от ненадёжного преобразования и сверки через `int`, добавить строгую валидацию формата и значений.
- [ ] Проверять прогресс сериалов на уровне бизнес-логики и БД, а не только интерфейса; исключить отрицательные номера эпизодов и некорректные переходы.
- [ ] Добавить глобальный обработчик неожиданных ошибок вокруг polling, чтобы неизвестная ошибка не останавливала бота и не приводила к потере пользовательских FSM-состояний.
- [ ] Разбить перегруженный модуль TMDB на несколько модулей с чёткими зонами ответственности.
- [ ] Удалить устаревшие общие handlers и DB-модули (например, `start`, `queries` и другие остатки старой структуры), поскольку проект перешёл на модульную архитектуру.
- [ ] Настроить CI в GitHub Actions.
- [ ] Подключить форматтер и линтер (`ruff`), зафиксировать правила в `pyproject.toml` и запускать проверки локально и в CI.
- [ ] Добавить Docker-конфигурацию для запуска приложения.
- [ ] Добавить безопасный `.env.example` со всеми поддерживаемыми переменными окружения и пояснениями без реальных секретов.
- [ ] Оценить необходимость Redis для хранения FSM-состояний с учётом небольшого объёма данных; при необходимости подключить его.
- [ ] Оценить необходимость перехода с SQLite на PostgreSQL: нагрузку, конкурентную запись, развёртывание и резервное копирование; при переходе подготовить драйвер, слой совместимых запросов и миграцию данных.
- [ ] Полностью переработать README: установка, конфигурация, запуск, архитектура и разработка.
- [ ] Добавить файл `LICENSE` с лицензией MIT и указать лицензию в README и метаданных проекта, чтобы условия копирования и использования кода были однозначными.
