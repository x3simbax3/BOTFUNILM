# TODO

- [x] Сделать безопасный разбор callback-данных: отказаться от ненадёжного преобразования и сверки через `int`, добавить строгую валидацию формата и значений.
- [x] Проверять прогресс сериалов на уровне бизнес-логики и БД, а не только интерфейса; исключить отрицательные номера эпизодов и некорректные переходы.
- [x] Добавить глобальный обработчик неожиданных ошибок вокруг polling, чтобы неизвестная ошибка не останавливала бота и не приводила к потере пользовательских FSM-состояний.
- [ ] Переделать кнопки фильтров библиотеки и их поведение: сейчас фильтры переключаются не так, как задумано.
- [x] Разбить перегруженный модуль TMDB на несколько модулей с чёткими зонами ответственности.
- [x] Вынести все пользовательские тексты из handlers, services и остальных модулей в единый слой представления; разделить тексты по функциональным модулям и подготовить каталог `lang/` с поддержкой локализации.
- [x] Удалить устаревшие общие handlers и DB-модули (например, `start`, `queries` и другие остатки старой структуры), поскольку проект перешёл на модульную архитектуру.
- [ ] Настроить CI в GitHub Actions.
- [ ] Подключить форматтер и линтер (`ruff`), зафиксировать правила в `pyproject.toml` и запускать проверки локально и в CI.
- [ ] Добавить Docker-конфигурацию для запуска приложения.
- [x] Добавить безопасный `.env.example` со всеми поддерживаемыми переменными окружения и пояснениями без реальных секретов.
- [ ] Оценить необходимость Redis для хранения FSM-состояний с учётом небольшого объёма данных; при необходимости подключить его.
- [ ] Оценить необходимость перехода с SQLite на PostgreSQL: нагрузку, конкурентную запись, развёртывание и резервное копирование; при переходе подготовить драйвер, слой совместимых запросов и миграцию данных.
- [ ] Полностью переработать README: установка, конфигурация, запуск, архитектура и разработка.
- [x] Добавить файл `LICENSE` с лицензией MIT и указать лицензию в README и метаданных проекта, чтобы условия копирования и использования кода были однозначными.
- [ ] Проверить семантику спецэпизодов и сезона `0` в TMDB: входят ли они в `number_of_episodes`, и определить единое правило их учёта в общем прогрессе сериала, существуют ли эти эпизоды на самом деле.
